import Modal from "../resources/js/Components/Modal";

export default {
    title: 'GoodJob/Modals/Modal',
    component: Modal,
    argTypes: {
    }
}

const Template = (arg) => <Modal {...arg}/>;

export const Default = Template.bind({});
Default.args = {
};




